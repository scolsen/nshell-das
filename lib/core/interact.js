/**
 * Created by scolsen on 2/21/17.
 */
/*function optset(options, proc, validOptions){ //takes validoptions of the process definition and prompts the user to set each value.
 let keys = [];
 let i = 0;
 for (let k in validOptions){
 keys[i] = k;
 i++
 }
 console.log(keys);
 let l = 0; //hoisted
 console.log("Enter an option for: " + keys[l]);
 prompt.consoleLine.on('line', function(line){
 options[keys[l]] = line;
 l++;
 if(keys[l] == undefined && keys[l] != 'interactive'){
 prompt.consoleLine.pause();
 validate(options, proc, validOptions);
 } else {
 console.log("Enter an option for: " + keys[l]);
 }
 });
 }*/

/*function ivalidate (options, proc, validOptions){
 optset(options, proc, validOptions);
 }*/